fn fib(n) {
  if (n < 2) {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

let results = [];

let i = 0;
let up_to = 32;

while (i < up_to){
  let c = fib(i);
  push(results, c);
  println(i, "/", up_to, " ", c);
  i += 1;
}

push(results, fib(i));

return results;
