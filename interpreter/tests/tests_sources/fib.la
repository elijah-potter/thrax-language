// This is a really slow implementation of fib.

fn fib(n) {
  if (n < 2) {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

let results = [];

let i = /* this is a block comment */ 0;
let up_to = 28;

while (i < up_to){
  let c = fib(i);
  push(results, c);
  println(i + 1, "/", up_to, " ", c);
  i += 1;
}

return results;
